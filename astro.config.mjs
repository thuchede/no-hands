import { defineConfig } from "astro/config";

// https://astro.build/config
export default defineConfig({
	// TODO: set to your GitHub Pages origin before deploying, e.g. "https://<user>.github.io"
	site: "https://example.github.io",
	base: "/no-hands",
});
